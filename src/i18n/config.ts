const languages = [ "en", "ar" ];
const defaultLanguage = "ar";

const namespaces = [ "metadata", "config", "common" ];
const defaultNamespace = "common";

const languageCookieName = "i18next";

export {
  languages,
  defaultLanguage,
  namespaces,
  defaultNamespace,
  languageCookieName,
};